# Post-Quantum Cryptography (CRYSTALS-Kyber-768 / NIST FIPS 203)
kyber-py>=1.2.0

# Classical Cryptography (ECDH, AES-256-GCM)
cryptography>=41.0.0

# Packet Capture (optional — required for full VPN mode)
scapy>=2.5.0

# Testing
pytest>=7.0.0
